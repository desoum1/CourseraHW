class Solution {
    public int[][] construct2DArray(int[] original, int m, int n) {
        //m is row
        //n is collum
        new array = 2darray[m][n];
        for (int z = 0; z< int[].length; z++)
            for (int x = 0; x < m; x++){
                for (int y = 0; y < n; y++)
                    if (2darray[m][n] == null)
                        2darray[m][n] = int[x];
            }

        }
    }
